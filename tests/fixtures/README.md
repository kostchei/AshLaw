# Test fixtures

Golden JSON, rulebook examples, and seeded fuzz corpora belong here.

`rules/` contains deterministic `AttackRequest`/expected-result fixtures. Retired or
internally inconsistent rulebook examples must not be added as fixtures.
